#foreach(Asm pn in news_c)
#set(Date date = pn.eventDate())
<li>
    <span class="date">${date.format2("dd")} ${date.format2("LMMMMM")} ${date.format2("yyyy")}</span>
    <span class="faculty">${pn.asm("np_hname")}</span>

    <p><a href="$!{pn.link}">${pn.asm("annotation")}</a></p>
</li>
#end
