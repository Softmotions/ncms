<?xml version="1.0" ?>
<schema name="core" version="1.1">

    <!-- required by solr -->
    <field name="_version_" type="long" indexed="true" stored="true"/>

    <!-- general -->
    <field name="id" type="string" indexed="true" stored="true" multiValued="false" required="true"/>
    <field name="type" type="text_general" indexed="true" stored="true" multiValued="false"/>
    <field name="published" type="bool" indexed="true" stored="true" multiValued="false"/>

    <!-- asm fileds -->
    <field name="name" type="text_general" indexed="true" stored="true" multiValued="false"/>
    <field name="description" type="text_general" indexed="true" stored="true" multiValued="false"/>

    <!-- dynamic fields for asm attrs -->
    <dynamicField name="asm_attr_*" type="text_general" indexed="true" stored="true" multiValued="true"/>

    <!-- default search field-->
    <field name="text" type="text_general" indexed="true" stored="false" multiValued="true"/>
    <defaultSearchField>text</defaultSearchField>

    <!--copy text fields to default search -->
    <copyField source="name" dest="text"/>
    <copyField source="asm_attr_*" dest="text"/>

    <!-- field to use to determine and enforce document uniqueness. -->
    <uniqueKey>id</uniqueKey>

    <!-- SolrQueryParser configuration: defaultOperator="AND|OR" -->
    <!--<solrQueryParser defaultOperator="OR"/>-->

    <fieldtype name="string" class="solr.StrField" sortMissingLast="true" omitNorms="true"/>
    <fieldType name="long" class="solr.TrieLongField" precisionStep="0" positionIncrementGap="0"/>
    <fieldType name="bool" class="solr.BoolField"/>

    <fieldType name="text_general" class="solr.TextField" positionIncrementGap="100">
        <analyzer type="index">
            <tokenizer class="solr.StandardTokenizerFactory"/>
            <filter class="solr.LowerCaseFilterFactory"/>
            <filter class="solr.EdgeNGramFilterFactory" minGramSize="2" maxGramSize="15"/>
            <filter class="solr.ReverseStringFilterFactory" />
            <filter class="solr.EdgeNGramFilterFactory" minGramSize="2" maxGramSize="15"/>
            <filter class="solr.ReverseStringFilterFactory" />
            <filter class="solr.SnowballPorterFilterFactory" language="Russian"/>
        </analyzer>
        <analyzer type="query">
            <tokenizer class="solr.StandardTokenizerFactory"/>
            <filter class="solr.LowerCaseFilterFactory"/>
            <filter class="solr.SnowballPorterFilterFactory" language="Russian"/>
        </analyzer>
    </fieldType>

</schema>

