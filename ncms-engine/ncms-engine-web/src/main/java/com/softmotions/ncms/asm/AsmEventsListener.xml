<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.softmotions.ncms.asm.AsmEventsListener">

    <update id="fixCoreFolderLocation" parameterType="map">
        UPDATE asm_cores c
        SET c.location = #{new_prefix} || SUBSTRING(c.location, #{prefix_like_len})
        WHERE c.location LIKE #{prefix_like}
    </update>

    <update id="fixCoreLocation" parameterType="map">
        UPDATE asm_cores c
        SET c.location = #{new_location}
        WHERE c.location = #{old_location}
    </update>

</mapper>